package stdioImporter.runtime;

/*Generated by MPS */

import stdio_parser.CodeGenerator;
import java.io.Reader;
import stdio_parser.parser;
import java_cup.Lexer;

public class ParserAdapter {
  public static CodeGenerator Parse(Reader r) {
    parser p = new parser(new Lexer(r));
    try {
      p.parse();
      return p.cg;
    } catch (Exception e) {
      System.out.println("Parser failed to parse: " + e.getMessage());
      return null;
    }
    // <node> 
  }
}
